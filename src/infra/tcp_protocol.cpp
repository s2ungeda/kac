#include "arbitrage/infra/tcp_protocol.hpp"

// All protocol types are header-only (inline functions, packed structs).
// This translation unit ensures the header compiles standalone and
// provides a place for future non-inline protocol helpers.

namespace arbitrage {
}  // namespace arbitrage
